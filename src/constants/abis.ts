export const CURVE_FACTORY_POOL_ABI = [
  "function balanceOf(address) view returns (uint256)",
  "function totalSupply() view returns (uint256)",
  "function get_balances() view returns (uint256[2])",
]

export const USDC_ABI = ["function decimals() view returns (uint8)"]

export const CUSD_ABI = ["function balanceOf(address) view returns(uint256)"]
